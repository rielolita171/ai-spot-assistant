import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rielolita.spottracker',
  appName: 'AI Spot Tracker',
  webDir: 'dist'
};

export default config;
